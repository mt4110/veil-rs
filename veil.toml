[core]
include = ["."]
ignore = [".git", "target", "vendor"]
max_file_size = 1000000 # 1MB

[masking]
placeholder = "<REDACTED>"

# Custom Rules (Example)
# [rules.my_internal_id]
# pattern = "PROJ-\\d{4}"
# severity = "high"
# score = 80
# category = "internal"

# Override Builtin Defaults
# [rules.jp_my_number]
# severity = "low"
# score = 10
