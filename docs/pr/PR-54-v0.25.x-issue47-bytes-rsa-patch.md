# PR54 — v0.25.x — Issue #47 Remediation (bytes/rsa patch updates)

## ✅ Goal

Resolve a subset of Dependabot alerts tracked in **Issue #47** by applying **low-risk transitive patch updates**:

- bytes: 1.11.0 → 1.11.1
- rsa:   0.9.9  → 0.9.10

This PR is intentionally minimal: **lockfile-centered**, no feature work, no refactors.

## 🔗 References

- Issue #47: https://github.com/mt4110/veil-rs/issues/47
- PR53 (merged): https://github.com/mt4110/veil-rs/pull/53

## 📌 Scope

### In
- Cargo.lock update driven by:
  - `cargo update -p bytes --precise 1.11.1` (NOTE: found already at 1.11.1)
  - `cargo update -p rsa --precise 0.9.10` (NOTE: dependency removed; no action needed)
- Smallest necessary code changes *only if* patch update breaks compilation/tests (unlikely, but allowed).

### Out
- Any dependency bumps beyond the two targets (unless forced by resolver, and then must be explained).
- Any behavior change / feature / cleanup.
- Any broad reformatting.

## 🧠 Working Theory / Risk

Patch updates should be API-compatible by semver; expected risk is low.
Actual risk is supply-chain / resolver drift; therefore we enforce:
- deterministic evidence (prverify + archived report)
- minimal diff
- explicit verification steps

## ✅ Always Run Contract (PR54)

- SOT exists (this file)
- plan/task exist:
  - docs/pr/PR-54-v0.25.x-issue47-bytes-rsa-patch/implementation_plan.md
  - docs/pr/PR-54-v0.25.x-issue47-bytes-rsa-patch/task.md
- `nix run .#prverify` PASS
- prverify evidence archived:
  - `docs/evidence/prverify/prverify_<UTC>_<sha7>.md`
- doc-links guard: docs must not contain raw `file://` style strings

## 🧾 Evidence

- Latest prverify report: `docs/evidence/prverify/prverify_20260211T041814Z_6c4a3bd.md`
- Notes:
  - If evidence inclusion ever triggers doc-links guard, sanitize only the offending strings while preserving meaning.

## ✅ Acceptance Criteria

1. Cargo.lock contains:
   - bytes 1.11.1
   - (rsa is absent/removed)
2. `cargo test --workspace` PASS
3. `nix run .#prverify` PASS
4. Evidence archived + SOT updated to point to it
5. PR diff remains minimal and reviewable (no unrelated changes)

## 🧭 Rollback

If anything unexpected happens:
- revert Cargo.lock change
- or use `cargo update -p <crate> --precise <old>` to restore
- regenerate evidence and keep a clean audit trail
