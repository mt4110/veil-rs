# v0.20.0 Planning: Command Test Matrix

## 北極星
**全コマンドが契約化され、CIで守れる**

## 完了条件
1. **Command Test Matrix v1** が以下2形式で存在する
    - `docs/v0.20.0-planning/command_test_matrix.md`
    - `docs/v0.20.0-planning/command_test_matrix.csv`
2. **Matrix 網羅率 100%**（= *clap help に出るコマンド/サブコマンド* の網羅。※未実装/無効化は別枠で追跡）
3. **Smoke Suite (P0) が実装され、CIで常時緑**（trycmd）
    - `veil --help` ✅
    - `veil --version` ✅
    - `veil scan <clean-fixture> --format json` ✅
    - `cat <stdin> | veil filter` ✅
    - `veil mask --dry-run <path>` ✅
    - `veil config check` ✅
    - `veil init (minimal)` 🔜（TODO: 安定した非対話セットアップ）
    - `veil guardian (offline-safe)` 🔜（TODO: cache / CI方針）
4. **E2E Golden Tests (P0)** が数本実装されている

## 非ゴール
- 新機能追加
- 外部依存の完全E2E（方針未決なら「契約定義」だけでもOK）

## Backlog / Epic候補
- **SOT コマンド（new/rename）**
  - 現状 `cli.rs` / `commands/` に存在しないため、Matrix網羅率には含めない。
  - v0.20.0 で復活させるなら *P0 Epic* として別PRで実装 → Matrixへ追加。

## 成果物
- [x] `command_test_matrix.md`
- [x] `command_test_matrix.csv`
- [x] `crates/veil-cli/tests/cmd/`（trycmd Smoke Suite）
- [x] `crates/veil-cli/tests/cmd/fixtures/`（ダミー入力）
- [ ] `tests/golden/`（必要になったら追加：まずはSmokeを強くする）
