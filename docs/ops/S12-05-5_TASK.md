- [x] (Discovery) rg_prverify.txt から prverify 入口ファイルの 実パス確定（OBSに残っていること）
- [x] (SOT) docs/ops/S12-05-5_PLAN.md / TASK.md 作成（このPRの憲法を明記）
- [x] (SOT) docs/ops/STATUS.md に S12-05.5 行追加（1% (WIP) / Last Updated 更新）
- [x] (Design) prverifyに --mode local-fast / --parallel / --cache-dir を追加（デフォfull不変）
- [x] (Impl) change-scope 判定（git diff 기반）を実装
  - base_sha 解決（origin/main → main の順）
  - 不確実なら fullへフォールバック（SKIPで誤魔化さない）
- [x] (Impl) キャッシュ固定（.local/cache/...）
  - Go: GOCACHE（+必要なら GOMODCACHE）
  - Rust: CARGO_TARGET_DIR
- [x] (Impl) safe parallel（最大2、デフォ1、上限超えたら2へ丸めて ERROR: ログ）
- [x] (Obs) .local/obs/... に step duration を OK: 行で出す（KEY=VALUE）
- [x] (Verify-Light) ローカル軽検証（重いのはここでは回さない）
  - nix run .#prverify（full）→ 既存のPASS/FAIL出力が崩れてない
  - nix run .#prverify -- --mode local-fast → 期待通り SKIP: が出る
- [x] (Verify-Heavy / 分割) 必要なら go/cargo を個別に回す（同時に回さない）
- [x] (PR Body) SOT/証拠スタイル（ガチガチ版＋短縮版）で Evidence 目次と OBS パスを記載
- [x] (Closeout) マージ儀式→ branch delete → STATUS 100% (Merged) 更新
