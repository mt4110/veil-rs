# S12-05.6 TASK — prverify stopless hardening

**Branch**: `s12-05-6-prverify-stopless-fix-v1`
**PR Title**: `S12-05.6: prverify stopless hardening (no os.Exit + bugfixes)`

---

## Tasks (順序固定)

- [x] Audit: dep-guard `cmd.Run()` 二重実行がないことを確認 → SKIP: already single-run (L339)
- [x] Audit: `git diff` の `cmd.Dir=root` 設定済みを確認 → SKIP: already set (L810)
- [x] `cmd/prverify/main.go` の `os.Exit(1)` 撤去（L437: `hasError` フラグ + `PASS:` 出力へ置換）
- [x] `OK: phase=end stop=0` のハードコードを削除 → `stop=%d` で `hasError` 由来の値を出力
- [x] `ERROR:` を stdout 基準に統一（全箇所: stderr にも出すが stdout が正）
- [ ] `docs/ops/S12-05-6_PLAN.md` 作成
- [ ] `docs/ops/S12-05-6_TASK.md` 作成（本ファイル）
- [ ] `docs/ops/STATUS.md` 更新（S12-05.6 行追加: 1% → PR open → 99% → Merged）
- [ ] 軽検証: `go build ./cmd/prverify`
- [ ] 軽検証: `nix run .#prverify -- --mode local-fast --parallel 1`
- [ ] PR 本文作成 + push（**マージ禁止**）
- [ ] CI 確認（11 checks PASS）
