# ==============================================================================
# entropy.toml — veil デフォルトルール (スキーマ v1)
# 生成されたシード (組み込み移行ベースライン) — 2025-12-29
#
# 注意:
# - このファイルは "default" ルールパックの一部です。順序は
#   このディレクトリの 00_manifest.toml によって制御されます。
# - パターンはエンジンによって解釈される正規表現文字列です (Rust regex)。
# - このシードは、現在の組み込みルールと動作が同一であることを目指しています。
#
# なぜ空なのか？
# - 現在の組み込みベースライン (このリポジトリのスナップショット内) は
#   正規表現ルールとしてエントロピーベースのルールを定義していません。エントロピーは通常、
#   別のヒューリスティックエンジン (閾値、トークン化、許可リスト) です。
# - そのため、動作を変更しないように、このファイルをプレースホルダーとして維持します。
# ==============================================================================

[pack]
id = "default.entropy"
schema_version = 1
version = 1
description = "veilデフォルトパックのエントロピールール"

# (組み込みベースラインにはこのカテゴリのルールはまだありません)
# 準備ができたら [[rules]] にルールを追加してください。
#
# 例:
# [[rules]]
# id = "entropy.generic.high"
# description = "高エントロピートークン (一般的なヒューリスティック)"
# pattern = '''...'''
# severity = "MEDIUM"
# score = 50
# category = "secret"
# tags = ["entropy"]
