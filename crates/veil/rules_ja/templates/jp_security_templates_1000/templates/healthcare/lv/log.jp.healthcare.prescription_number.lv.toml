# JP security-risk detection template.
# Generated as one-rule TOML for selective adoption and easy deletion.

[[rules]]
id = "log.jp.healthcare.prescription_number.lv"
pattern = "(?i)(?:prescription_number|処方箋番号|処方番号)[\\s　]*(?:[:：=＝]|=>|->|は|：|[\\s　]+)[\\s　]*[\"「『【\\[]?(?:[A-Za-z0-9０-９_\\-]{4,80})[\"」』】\\]]?"
description = "処方箋番号 - 日本語ラベル付き値"
severity = "high"
score = 88
base_score = 88
category = "healthcare"
tags = ["log", "jp", "healthcare", "label_value", "prescription"]
placeholder = "<REDACTED:HEALTH>"
context_lines_before = 1
context_lines_after = 1
