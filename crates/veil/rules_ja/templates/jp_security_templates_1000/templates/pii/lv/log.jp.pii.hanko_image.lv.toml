# JP security-risk detection template.
# Generated as one-rule TOML for selective adoption and easy deletion.

[[rules]]
id = "log.jp.pii.hanko_image.lv"
pattern = "(?i)(?:hanko_image|seal_image|ハンコ画像|印鑑画像|押印画像|印影)[\\s　]*(?:[:：=＝]|=>|->|は|：|[\\s　]+)[\\s　]*[\"「『【\\[]?(?:https?://[^\\s　<>\"\\)）\\]\\}]+)[\"」』】\\]]?"
description = "印影・ハンコ画像 - 日本語ラベル付き値"
severity = "high"
score = 88
base_score = 88
category = "pii"
tags = ["log", "jp", "pii", "label_value", "identity", "hanko"]
placeholder = "<REDACTED:PII>"
context_lines_before = 1
context_lines_after = 1
