# JP security-risk detection template.
# Generated as one-rule TOML for selective adoption and easy deletion.

[[rules]]
id = "log.jp.pii.biometric_face.schema"
pattern = "(?i)(?:(?:列名|カラム|フィールド|ヘッダー|CSV|DB|テーブル|table|column|field|schema|スキーマ)[^\\r\\n]{0,40}(?:face_embedding|face_vector|顔認証データ|生体認証顔|顔特徴量)|(?:face_embedding|face_vector|顔認証データ|生体認証顔|顔特徴量)[^\\r\\n]{0,24}(?:列|カラム|フィールド|ヘッダー|column|field))"
description = "顔認証データ - CSV/DBスキーマ上の機微カラム名"
severity = "critical"
score = 77
base_score = 77
category = "pii"
tags = ["log", "jp", "pii", "schema_marker", "biometric", "face"]
placeholder = "<REDACTED:PII>"
context_lines_before = 0
context_lines_after = 0
