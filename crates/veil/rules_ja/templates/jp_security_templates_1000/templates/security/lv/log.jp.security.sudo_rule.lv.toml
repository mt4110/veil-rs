# JP security-risk detection template.
# Generated as one-rule TOML for selective adoption and easy deletion.

[[rules]]
id = "log.jp.security.sudo_rule.lv"
pattern = "(?i)(?:sudo_rule|sudo設定|sudo権限)[\\s　]*(?:[:：=＝]|=>|->|は|：|[\\s　]+)[\\s　]*[\"「『【\\[]?(?:[A-Za-z0-9０-９ぁ-んァ-ヶ一-龠々〆〇ー・（）()\\[\\]＿_\\-]{2,120})[\"」』】\\]]?"
description = "sudo設定 - 日本語ラベル付き値"
severity = "high"
score = 88
base_score = 88
category = "security"
tags = ["log", "jp", "security", "label_value", "sudo"]
placeholder = "<REDACTED:SECURITY>"
context_lines_before = 1
context_lines_after = 1
